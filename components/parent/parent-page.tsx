import { ParentAttendance, ParentPayment, ParentStudent } from "@/lib/types";
import { formatMoney } from "@/lib/format";
export function ParentPage({ parentStudent, parentPayments, parentAttendance }: { parentStudent: ParentStudent | null; parentPayments: ParentPayment[]; parentAttendance: ParentAttendance[]; }) {
  if (!parentStudent) return <div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Ата-ана кабинеті</h2><p className="mt-2 text-sm text-slate-500">Code + PIN арқылы кіреді.</p></div>;
  return <div className="space-y-6"><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-2xl font-bold">{parentStudent.name}</h2><div className="mt-3 flex flex-wrap gap-2"><span className="rounded-full bg-slate-100 px-3 py-1 text-sm">Қарыз: {formatMoney(parentStudent.debt_amount)}</span><span className="rounded-full bg-slate-100 px-3 py-1 text-sm">Айлық: {formatMoney(parentStudent.monthly_fee)}</span></div></div><div className="grid grid-cols-1 gap-6 xl:grid-cols-2"><div className="rounded-2xl border bg-white p-5 shadow-sm"><h3 className="text-lg font-semibold">Төлем тарихы</h3><div className="mt-4 space-y-3">{parentPayments.map((payment) => <div key={payment.id} className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-3"><span>{payment.created_at?.slice(0,10) || "-"}</span><span className="font-semibold">{formatMoney(payment.amount)}</span></div>)}</div></div><div className="rounded-2xl border bg-white p-5 shadow-sm"><h3 className="text-lg font-semibold">Қатысу</h3><div className="mt-4 space-y-3">{parentAttendance.map((item) => <div key={item.id} className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-3"><span>{item.lesson_date || item.created_at?.slice(0,10) || "-"}</span><span className={item.status === "Келді" ? "font-semibold text-emerald-600" : "font-semibold text-amber-600"}>{item.status}</span></div>)}</div></div></div></div>;
}