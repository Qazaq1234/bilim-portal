import { AttendanceRow, StudentRow } from "@/lib/types";
type AttendanceForm = { student_id: string; lesson_date: string; status: string; note: string; };
export function AttendancePage({ attendance, students, attendanceLoading, attendanceError, attendanceForm, setAttendanceForm, onCreateAttendance, onDeleteAttendance }: { attendance: AttendanceRow[]; students: StudentRow[]; attendanceLoading: boolean; attendanceError: string; attendanceForm: AttendanceForm; setAttendanceForm: React.Dispatch<React.SetStateAction<AttendanceForm>>; onCreateAttendance: () => void; onDeleteAttendance: (id: string) => void; }) {
  return <section className="grid grid-cols-1 gap-6 xl:grid-cols-[minmax(0,0.8fr)_minmax(0,1.2fr)]"><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Қатысу белгілеу</h2><div className="mt-4 grid gap-3"><select value={attendanceForm.student_id} onChange={(e) => setAttendanceForm((s) => ({ ...s, student_id: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm"><option value="">Оқушы таңда</option>{students.map((s) => <option key={s.id} value={s.id}>{s.name}</option>)}</select><input type="date" value={attendanceForm.lesson_date} onChange={(e) => setAttendanceForm((s) => ({ ...s, lesson_date: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm" /><select value={attendanceForm.status} onChange={(e) => setAttendanceForm((s) => ({ ...s, status: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm"><option>Келді</option><option>Келмеді</option><option>Себепті</option></select><textarea value={attendanceForm.note} onChange={(e) => setAttendanceForm((s) => ({ ...s, note: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm" placeholder="Ескерту" /><button onClick={onCreateAttendance} className="rounded-xl bg-blue-600 px-4 py-2 text-sm font-medium text-white">Сақтау</button>{attendanceError && <div className="rounded-xl bg-rose-50 px-3 py-3 text-sm text-rose-700">{attendanceError}</div>}</div></div><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Қатысу журналы</h2>{attendanceLoading ? <div className="mt-4 text-sm text-slate-500">Жүктеліп жатыр...</div> : <div className="mt-4 space-y-3">{attendance.map((item) => { const student = students.find((s) => s.id === item.student_id); return <div key={item.id} className="flex items-center justify-between rounded-2xl border p-4"><div><div className="font-medium">{student?.name || "Оқушы"}</div><div className="text-sm text-slate-500">{item.lesson_date}</div></div><div className="flex items-center gap-3"><span className={item.status === "Келді" ? "font-semibold text-emerald-600" : "font-semibold text-amber-600"}>{item.status}</span><button onClick={() => onDeleteAttendance(item.id)} className="rounded-xl border px-3 py-2 text-sm font-medium hover:bg-slate-50">Өшіру</button></div></div>; })}</div>}</div></section>;
}