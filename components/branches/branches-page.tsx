import { BranchRow } from "@/lib/types";
type BranchForm = { name: string; city: string; address: string; };
export function BranchesPage({ branches, branchesLoading, branchesError, branchForm, setBranchForm, onCreateBranch, onDeleteBranch }: { branches: BranchRow[]; branchesLoading: boolean; branchesError: string; branchForm: BranchForm; setBranchForm: React.Dispatch<React.SetStateAction<BranchForm>>; onCreateBranch: () => void; onDeleteBranch: (id: string) => void; }) {
  return <section className="grid grid-cols-1 gap-6 xl:grid-cols-[minmax(0,0.8fr)_minmax(0,1.2fr)]"><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Филиал қосу</h2><div className="mt-4 grid gap-3"><input value={branchForm.name} onChange={(e) => setBranchForm((s) => ({ ...s, name: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm" placeholder="Филиал атауы" /><input value={branchForm.city} onChange={(e) => setBranchForm((s) => ({ ...s, city: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm" placeholder="Қала" /><input value={branchForm.address} onChange={(e) => setBranchForm((s) => ({ ...s, address: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm" placeholder="Мекенжай" /><button onClick={onCreateBranch} className="rounded-xl bg-blue-600 px-4 py-2 text-sm font-medium text-white">Сақтау</button>{branchesError && <div className="rounded-xl bg-rose-50 px-3 py-3 text-sm text-rose-700">{branchesError}</div>}</div></div><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Филиалдар</h2>{branchesLoading ? <div className="mt-4 text-sm text-slate-500">Жүктеліп жатыр...</div> : <div className="mt-4 space-y-3">{branches.map((branch) => <div key={branch.id} className="flex items-center justify-between rounded-2xl border p-4"><div><div className="font-medium">{branch.name}</div><div className="text-sm text-slate-500">{branch.city || "-"}</div></div><button onClick={() => onDeleteBranch(branch.id)} className="rounded-xl border px-3 py-2 text-sm font-medium hover:bg-slate-50">Өшіру</button></div>)}</div>}</div></section>;
}