import { PaymentRow, AttendanceRow } from "@/lib/types";
import { formatMoney } from "@/lib/format";
export function StudentCard({ student, payments, attendance, onBack }: { student: { id: string; name: string; branch: string; teacher: string; fee: number; debt: number; code: string; pin: string; } | null; payments: PaymentRow[]; attendance: AttendanceRow[]; onBack: () => void; }) {
  if (!student) return <div className="rounded-2xl border bg-white p-5 shadow-sm"><div className="text-slate-500">Оқушы таңдалмаған</div></div>;
  const studentPayments = payments.filter((p) => p.student_id === student.id);
  const studentAttendance = attendance.filter((a) => a.student_id === student.id);
  return <div className="space-y-6"><div className="flex items-center justify-between"><div><h2 className="text-2xl font-bold">{student.name}</h2><p className="text-sm text-slate-500">{student.branch} · {student.teacher}</p></div><button onClick={onBack} className="rounded-xl border px-4 py-2 text-sm font-medium hover:bg-slate-50">Артқа</button></div><div className="grid grid-cols-1 gap-4 md:grid-cols-4"><div className="rounded-2xl border bg-white p-4 shadow-sm"><div className="text-sm text-slate-500">Айлық</div><div className="mt-2 text-xl font-bold">{formatMoney(student.fee)}</div></div><div className="rounded-2xl border bg-white p-4 shadow-sm"><div className="text-sm text-slate-500">Қарыз</div><div className="mt-2 text-xl font-bold">{formatMoney(student.debt)}</div></div><div className="rounded-2xl border bg-white p-4 shadow-sm"><div className="text-sm text-slate-500">Code</div><div className="mt-2 text-xl font-bold">{student.code}</div></div><div className="rounded-2xl border bg-white p-4 shadow-sm"><div className="text-sm text-slate-500">PIN</div><div className="mt-2 text-xl font-bold">{student.pin}</div></div></div><div className="grid grid-cols-1 gap-6 xl:grid-cols-2"><div className="rounded-2xl border bg-white p-5 shadow-sm"><h3 className="text-lg font-semibold">Төлем тарихы</h3><div className="mt-4 space-y-3">{studentPayments.map((payment) => <div key={payment.id} className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-3"><span>{payment.created_at?.slice(0,10) || "-"}</span><span className="font-semibold">{formatMoney(payment.amount)}</span></div>)}</div></div><div className="rounded-2xl border bg-white p-5 shadow-sm"><h3 className="text-lg font-semibold">Қатысу тарихы</h3><div className="mt-4 space-y-3">{studentAttendance.map((item) => <div key={item.id} className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-3"><span>{item.lesson_date}</span><span className={item.status === "Келді" ? "font-semibold text-emerald-600" : "font-semibold text-amber-600"}>{item.status}</span></div>)}</div></div></div></div>;
}