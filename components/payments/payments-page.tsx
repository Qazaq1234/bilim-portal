import { PaymentRow, StudentRow } from "@/lib/types";
import { formatMoney } from "@/lib/format";
type PaymentForm = { student_id: string; amount: string; method: string; note: string; };
export function PaymentsPage({ payments, students, paymentsLoading, paymentsError, paymentForm, setPaymentForm, onCreatePayment, onDeletePayment }: { payments: PaymentRow[]; students: StudentRow[]; paymentsLoading: boolean; paymentsError: string; paymentForm: PaymentForm; setPaymentForm: React.Dispatch<React.SetStateAction<PaymentForm>>; onCreatePayment: () => void; onDeletePayment: (id: string) => void; }) {
  return <section className="grid grid-cols-1 gap-6 xl:grid-cols-[minmax(0,0.8fr)_minmax(0,1.2fr)]"><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Төлем енгізу</h2><div className="mt-4 grid gap-3"><select value={paymentForm.student_id} onChange={(e) => setPaymentForm((s) => ({ ...s, student_id: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm"><option value="">Оқушы таңда</option>{students.map((s) => <option key={s.id} value={s.id}>{s.name}</option>)}</select><input value={paymentForm.amount} onChange={(e) => setPaymentForm((s) => ({ ...s, amount: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm" placeholder="Сома" /><select value={paymentForm.method} onChange={(e) => setPaymentForm((s) => ({ ...s, method: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm"><option value="cash">cash</option><option value="kaspi">kaspi</option><option value="transfer">transfer</option><option value="card">card</option><option value="other">other</option></select><textarea value={paymentForm.note} onChange={(e) => setPaymentForm((s) => ({ ...s, note: e.target.value }))} className="rounded-xl border px-3 py-2 text-sm" placeholder="Ескерту" /><button onClick={onCreatePayment} className="rounded-xl bg-blue-600 px-4 py-2 text-sm font-medium text-white">Сақтау</button>{paymentsError && <div className="rounded-xl bg-rose-50 px-3 py-3 text-sm text-rose-700">{paymentsError}</div>}</div></div><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Төлемдер журналы</h2>{paymentsLoading ? <div className="mt-4 text-sm text-slate-500">Жүктеліп жатыр...</div> : <div className="mt-4 space-y-3">{payments.map((payment) => { const student = students.find((s) => s.id === payment.student_id); return <div key={payment.id} className="flex items-center justify-between rounded-2xl border p-4"><div><div className="font-medium">{student?.name || "Оқушы"}</div><div className="text-sm text-slate-500">{payment.created_at?.slice(0,10) || "-"} · {payment.method || "cash"}</div></div><div className="flex items-center gap-3"><div className="text-right font-semibold">{formatMoney(payment.amount)}</div><button onClick={() => onDeletePayment(payment.id)} className="rounded-xl border px-3 py-2 text-sm font-medium hover:bg-slate-50">Өшіру</button></div></div>; })}</div>}</div></section>;
}