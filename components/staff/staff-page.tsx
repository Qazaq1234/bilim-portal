import { ProfileRow } from "@/lib/types";
export function StaffPage({ staff, staffLoading, staffError }: { staff: ProfileRow[]; staffLoading: boolean; staffError: string; }) {
  return <section className="grid grid-cols-1 gap-6 xl:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]"><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Қызметкерлер</h2>{staffLoading ? <div className="mt-4 text-sm text-slate-500">Жүктеліп жатыр...</div> : <div className="mt-4 space-y-3">{staff.map((person) => <div key={person.user_id} className="flex items-center justify-between rounded-2xl border p-4"><div><div className="font-medium">{person.full_name}</div><div className="text-sm text-slate-500">{person.role}</div></div><span className="rounded-full bg-slate-100 px-3 py-1 text-sm">{person.status}</span></div>)}</div>}{staffError && <div className="mt-4 rounded-xl bg-rose-50 px-3 py-3 text-sm text-rose-700">{staffError}</div>}</div><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Team summary</h2><div className="mt-4 grid grid-cols-2 gap-4 text-sm"><div className="rounded-xl bg-slate-50 p-4">Director: {staff.filter((s) => s.role === "director").length}</div><div className="rounded-xl bg-slate-50 p-4">Teacher: {staff.filter((s) => s.role === "teacher").length}</div><div className="rounded-xl bg-slate-50 p-4">Admin: {staff.filter((s) => s.role === "admin").length}</div><div className="rounded-xl bg-slate-50 p-4">Active: {staff.filter((s) => s.status === "active").length}</div></div></div></section>;
}